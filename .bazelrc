# Enforce a consistent Java version for all builds.

# This sets the --source and --target flags for javac to "21".
build --java_language_version=21
build --java_runtime_version=21

# Enable the modern platforms and toolchains framework
build --enable_platform_specific_config
